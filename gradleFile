node('scripted') {
    stage('version control') {
      git url: 'https://github.com/99876543/spring-petclinic.git',
        branch: 'gradleFile'    
    }
}
    
     stage('Install Gradle') {
       steps {
         sh 'sudo apt install -y gradle'
         sh 'gradlew build'
    }
}
